import {NgModule} from '@angular/core';
import {CharacterDetailService} from './character-detail.service';

@NgModule({
  providers: [
    CharacterDetailService
  ]
})
export class CharacterDetailServicesModule {
}
